# 📊 АНАЛИЗ ДИСКОВОГО ПРОСТРАНСТВА

**Дата анализа:** 23 ноября 2025, 19:31 UTC  
**Статус:** ⚠️ КРИТИЧЕСКОЕ ЗАПОЛНЕНИЕ ДИСКА

---

## 🔴 ТЕКУЩЕЕ СОСТОЯНИЕ

### Использование диска:
```
Файловая система: /dev/vda1
Размер:           14 GB
Использовано:     13 GB (91%)
Свободно:         1.3 GB
```

**⚠️ КРИТИЧЕСКАЯ СИТУАЦИЯ:** Диск заполнен на 91%! Осталось всего 1.3 GB свободного места.

---

## 📁 РАСПРЕДЕЛЕНИЕ ПРОСТРАНСТВА

### Топ-10 самых больших директорий:

| Директория | Размер | % от диска |
|------------|--------|------------|
| **`/backup`** | **4.5 GB** | **32%** |
| `/public` | 136 MB | 1% |
| `/node_modules` | 60 MB | 0.4% |
| `/logs` | 37 MB | 0.3% |
| Остальное | < 1 MB | < 0.1% |

**Вывод:** Директория `backup` занимает **32% всего диска** и является основной причиной заполнения.

---

## 💾 АНАЛИЗ БЭКАПОВ

### Статистика:
- **Всего файлов бэкапов:** 35
- **Общий размер:** 4.5 GB
- **Средний размер файла:** ~131 MB
- **Период хранения:** 30 дней (настроено в скрипте)
- **Частота создания:** 2 раза в день (02:00 и 14:00)

### Временной диапазон:
- **Самый старый бэкап:** `delivery-backup-20251029_073828.tar.gz` (29 октября)
- **Самый новый бэкап:** `delivery-backup-20251115_020001.tar.gz` (15 ноября)
- **Период:** ~17 дней

### Прогноз:
При текущих настройках (30 дней хранения, 2 бэкапа в день):
- **Максимум бэкапов:** 60 файлов
- **Прогнозируемый размер:** ~8 GB
- **Это займет:** ~57% диска (при текущем размере диска 14 GB)

---

## 🔍 ПРОБЛЕМЫ

### 1. ⚠️ Избыточное хранение бэкапов
- **Проблема:** Хранение 30 дней при 2 бэкапах в день = 60 файлов
- **Размер:** ~8 GB (больше половины диска)
- **Риск:** Диск заполнится полностью через несколько дней

### 2. ⚠️ Нет проверки свободного места перед созданием бэкапа
- Скрипт проверяет использование диска только после создания бэкапа
- Нет превентивной очистки при нехватке места

### 3. ⚠️ Нет ротации по размеру
- Ротация только по времени (30 дней)
- Нет ограничения по общему размеру директории бэкапов

---

## ✅ РЕКОМЕНДАЦИИ

### Вариант 1: Уменьшить период хранения (РЕКОМЕНДУЕТСЯ)
**Изменить `RETENTION_DAYS` с 30 на 7-14 дней:**

```bash
# В файле backup-system.sh изменить:
RETENTION_DAYS=7  # или 14
```

**Результат:**
- При 7 днях: 14 файлов × 131 MB = ~1.8 GB
- При 14 днях: 28 файлов × 131 MB = ~3.7 GB
- Освободится: ~2.7 GB (при 7 днях) или ~0.8 GB (при 14 днях)

### Вариант 2: Уменьшить частоту бэкапов
**Создавать бэкап 1 раз в день вместо 2:**

```bash
# В crontab оставить только:
0 2 * * * /home/enclude/delivery-app/backup-system.sh
# Удалить: 0 14 * * * ...
```

**Результат:**
- При 30 днях: 30 файлов × 131 MB = ~3.9 GB
- Освободится: ~0.6 GB

### Вариант 3: Комбинированный подход (ОПТИМАЛЬНО)
**7 дней хранения + 1 бэкап в день:**

```bash
RETENTION_DAYS=7
# Cron: только 0 2 * * *
```

**Результат:**
- 7 файлов × 131 MB = ~917 MB (~0.9 GB)
- Освободится: ~3.6 GB
- Достаточно для восстановления на неделю назад

### Вариант 4: Добавить ротацию по размеру
**Дополнить скрипт проверкой общего размера:**

```bash
# Добавить в backup-system.sh после строки 107:
# Очистка по размеру (если больше 2GB, удалять самые старые)
MAX_BACKUP_SIZE_GB=2
while [ $(du -sm "$BACKUP_DIR" | cut -f1) -gt $((MAX_BACKUP_SIZE_GB * 1024)) ]; do
    OLDEST=$(ls -1t "$BACKUP_DIR"/delivery-backup-*.tar.gz | tail -1)
    log "🗑️  Удаляем старый бэкап (превышен лимит размера): $(basename $OLDEST)"
    rm -f "$OLDEST"
done
```

---

## 🚀 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ

### 1. Очистить старые бэкапы (если не нужны)
```bash
# Удалить бэкапы старше 7 дней:
find /home/enclude/delivery-app/backup -name "delivery-backup-*.tar.gz" -mtime +7 -delete

# Или вручную удалить самые старые:
# ls -1t /home/enclude/delivery-app/backup/*.tar.gz | tail -20 | xargs rm
```

**Ожидаемое освобождение:** ~2.5 GB

### 2. Обновить скрипт бэкапа
- Уменьшить `RETENTION_DAYS` до 7-14
- Добавить проверку свободного места перед созданием
- Добавить ротацию по размеру

### 3. Мониторинг
- Настроить алерты при заполнении диска > 85%
- Регулярно проверять размер директории backup

---

## 📈 ПРОГНОЗ

### Текущая ситуация:
- **Сейчас:** 4.5 GB бэкапов, 91% диска заполнено
- **Через 7 дней:** ~5.3 GB бэкапов, ~95% диска
- **Через 15 дней:** ~6.7 GB бэкапов, **диск заполнится полностью**

### После оптимизации (7 дней, 1 бэкап/день):
- **Через 7 дней:** ~0.9 GB бэкапов, ~85% диска
- **Стабильное состояние:** ~0.9 GB бэкапов

---

## 📝 ВЫВОДЫ

1. ✅ **Подтверждено:** Бэкапы действительно заполняют диск (4.5 GB из 13 GB)
2. ⚠️ **Критично:** Диск заполнен на 91%, осталось 1.3 GB
3. 🔧 **Решение:** Уменьшить период хранения до 7 дней и/или частоту до 1 раза в день
4. 📊 **Рекомендация:** Комбинированный подход (7 дней + 1 раз в день) освободит ~3.6 GB

---

## 🔗 СВЯЗАННЫЕ ФАЙЛЫ

- Скрипт бэкапа: `/home/enclude/delivery-app/backup-system.sh`
- Директория бэкапов: `/home/enclude/delivery-app/backup/`
- Логи бэкапов: `/var/log/delivery-app-backup.log`
- Cron настройки: `crontab -l`

